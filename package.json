{
  "name": "doctor_appointment_app",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "npm install --prefix backend && npm install --prefix frontend && npm run build --prefix frontend",
    "start": "node backend/server.js",
    "start:local": "cross-env DOTENV_CONFIG_PATH=backend/.env NODE_ENV=production node -r dotenv/config backend/server.js",
    "dev": "concurrently -n API,WEB -c auto \"npm --prefix backend run server\" \"npm --prefix frontend run dev\""
  },
  "devDependencies": {
    "concurrently": "^9.0.0",
    "cross-env": "^10.0.0",
    "dotenv": "^17.2.1"
  }
}
